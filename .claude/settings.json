{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Glob|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f graphify-out/graph.json ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"ALWAYS read graphify-out/GRAPH_REPORT.md before ANY file search or grep. Do not use Grep or Glob until you have read the graph report first.\"}}' || true"
          }
        ]
      }
    ]
  }
}